-- Marker migration: initial schema is complete
-- This is a no-op that records successful completion of the initial schema.
-- All 30 migrations (001-029 + this marker) constitute the v1.0 schema.
SELECT 1;
